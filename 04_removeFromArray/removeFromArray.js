const removeFromArray = function(arr, ...rem) {
const newArrr = []
// Loop through arr 
for (el of arr){
  if(!arr[el].in)
}
// for every index that doesn't include values from rem add to new arr
};

// Do not edit below this line
module.exports = removeFromArray;
